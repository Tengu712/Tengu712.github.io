---
---

<div class="content">
    <slot />
</div>
<style is:global>
    .content {
		box-sizing: border-box;
		padding: clamp(0.75rem, -6rem + 18vw, 3rem);
		width: 100%;
		word-wrap : break-word;
		overflow-wrap : break-word;
	}
	.content a {
		color: black;
	}
    .content a:hover {
        color: red;
    }
    .content b {
        color: red;
    }
    .content h1 {
        font-size: clamp(1.25rem, -1rem + 6vw, 2rem);
    }
    .content h2 {
        margin-top: 5rem;
        padding: 0.25rem 0.8rem;
        background: transparent;
        border-left: solid 0.5rem black;
        font-size: clamp(1.25rem, -0.25rem + 4vw, 1.75rem);
    }
    .content h3 {
        margin-top: 2rem;
    }
    .content p {
        line-height: 2rem;
        margin: 2rem 0;
    }
</style>
